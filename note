程序就是描述how to do  
解释型语言，例如Python，是通过程序理解你的程序并执行。有特殊计算能力。  
编译型语言，例如C，通过程序将你的程序翻译为机器语言，然后机器语言直接执行。有确定的运算性能。  
但语言本无解释/编译之分。  

 运算符（operator）+-
算子（operand）CONSTANT variable
表达式 a = a + 1

单目运算 -a 只有一个算子 
优先级高于*/ a*-b
赋值运算符从右到左 a=6
递增递减运算符 +1/-1
count ++ count值不变
++count  count+1后的值
    printf("%d ", i++); // 5
    printf("%d ", i);   // 6
    
    printf("%d ", ++i); // 6
    printf("%d ", i);   // 6


复合赋值 先右后左
total *= sum + 12

优先级：算数运算>关系运算>赋值运算
关系运算内，==与!=优先级较低
