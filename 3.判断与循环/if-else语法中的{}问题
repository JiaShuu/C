#include <stdio.h>

int main(){
    int a, b;
    
    printf("请输入两个整数：");
    scanf("%d %d", &a, &b);
    
    int max = 0;
    if (a > b) {
        max = a;
    } else 
        max = b;
        max = 555;                     // 缩进后也与下句一起执行
    printf("max = %d\n", max);
}
